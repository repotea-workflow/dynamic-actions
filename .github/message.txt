fix(mobile): make image Fit tool resize to label frame (#215) (#223)

The Fit action now applies proportional contain sizing and repositions the
selected image to fill the active frame, matching template editor expectations.
Split the mobile editor shell into focused modules to satisfy codeline limits.
