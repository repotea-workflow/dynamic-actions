feat(codeon): emit runtime_source on MCP issue AgentBinding JSON

MCP issue get now serializes identity as kind, source, and optional key.
HTTP projections reject inconsistent source+profile pairs. Named("default")
is unchanged. FORMAT_VERSION stays 1 because digests do not hash this JSON.

Co-authored-by: fewensa <fewensa@users.noreply.github.com>
