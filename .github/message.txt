fix(cursor): align preflight ACP command

codeon:
  version: 1
  authority: FWN-61
  description: |-
    Launch Cursor startup preflight with the same `acp --sandbox enabled` command used by the production adapter.
    
    Tighten adapter and daemon fixtures to reject the obsolete extra `agent` argument.
