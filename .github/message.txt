feat(templates): connect shared editor assets

Expose versioned font and icon catalogs to the Admin Plate host and Flutter app. Add snapshot migrations so paginated catalogs remain consistent across updates.\n\nRetain published asset versions and validate checksums so historical templates cannot silently render replacement content. Replace the mobile Plate shell with Gprinter-owned touch UI.
