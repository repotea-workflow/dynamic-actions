fix(codex): isolate empty capability manifests

codeon:
  version: 1
  authority: FWN-62
  description: |-
    Require Codex runtime isolation for every sealed capability manifest, including an empty allowlist. Persist the canonical empty MCP inventory digest as provider-accepted evidence when exact readback succeeds without optional degradation.
    
    Add a regression covering empty-manifest isolation and ProviderAccepted readback.
