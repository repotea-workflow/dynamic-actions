fix(runtime): close runnable lease review gaps

codeon:
  version: 1
  authority: FWN-51
  description: |-
    Bind Publication checks waiting to terminal Agent containment evidence in the existing owner transaction, and defer Review handoff until the attention is resolved and runnable capacity is reacquired with a fresh generation.
    
    Reconcile startup against the latest containment generation for every dispatch owned by an Attempt so older live Agent runtimes preserve held capacity or fail closed when a lease was released.
    
    Add canonical A-E safe/unsafe daemon coverage, older-runtime startup acceptance tests, and update the runtime specification and OpenWiki architecture context.
