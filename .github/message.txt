fix: isolate provider startup calls

codeon:
  version: 1
  authority: FWN-16
  description: |-
    Route every synchronous Codex App Server call through the shared bounded blocking-call supervisor, reserving cancellation capacity for exact goal and turn cancellation.
    
    Publish Cursor and Grok termination handles under dispatch-scoped startup references before initialization, so draining can cancel spawned provider processes before runtime checkpoints exist. Add deterministic concurrency and blocked-handshake coverage.
