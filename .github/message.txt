merge(main): repair lifecycle v2 integration

codeon:
  version: 1
  authority: FWN-18
  description: |-
    Merge refreshed origin/main into the frozen lifecycle v2 target while preserving the required two-parent history.
    
    Renumber the lifecycle re-arm and undispatched-cancellation migrations into unique forward slots and extend contiguous catalog acceptance through version 138.
    
    Retain an externally accepted Cursor continuation when lease re-arm fails, return Unknown for Attention handling, and suppress duplicate continuation sends. Select the earliest overdue idle or resource-ceiling deadline for retained and recovered Cursor lifecycle authority.
