feat(mobile): fetch and validate remote account templates

AccountTemplateRemoteReader GETs the detail, downloads the file and every referenced image through the typed clients, verifies size/checksum/envelope/frame/assets, rewrites ids to local draft assets, and atomically caches only after full validation under the captured lease. Dart client now decodes sourceTemplateVersionId. W3-A slice 2.
