fix(runtime): seal Agent Runtime Profiles

codeon:
  version: 1
  authority: FWN-61
  description: |-
    Persist canonical runtime identity snapshots with execution bindings and reject legacy or drifted replay. Route only startup-available provider profiles, expose sanitized per-profile health and capacity accounting, and enforce canonical trusted operator paths.
