fix(codeon): recover dirty revision retries

Revalidate frozen repository authority before replacing legacy operator-cancel attention so restart recovery can fence and retry the dispatch without manual intervention.
