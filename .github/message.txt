Drain live-e2e probe pipes concurrently

codeon:
  version: 1
  authority: FWN-1
  description: |-
    Wait-then-read left provider probes blocked once either OS pipe buffer filled, so the harness timed out healthy agents. Drain stdout and stderr while waiting and keep TERM/KILL on the process group; add a regression that emits past pipe capacity.
