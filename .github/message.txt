fix(runtime): supervise startup recovery

codeon:
  version: 1
  authority: fewensa-16
  description: |-
    Route durable operations that survive restart through provider-aware reconciliation inside the bounded dispatch-worker supervisor. Refresh exact operation, dispatch, and fence authority at the worker boundary so recovery results remain fenced and candidate-local read failures stay isolated.
    
    Replace broad Picker invariant fatal classification with explicit process-fatal authority for persistence integrity and essential daemon resources. Add restart recovery coverage that records an abandoned dispatch and reconstructs the execution runtime service.
