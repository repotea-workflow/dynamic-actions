Merge origin/main into worker supervision

codeon:
  version: 1
  authority: FWN-16
  description: |-
    Resolve landing-preflight merge conflicts while preserving dispatch-worker supervision, bounded shutdown, and the target branch's repository-baseline recovery. Adapt the merged runtime shutdown test to initialize the shared shutdown deadline.
