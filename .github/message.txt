fix(runtime): release provider startup handles

codeon:
  version: 1
  authority: FWN-16
  description: |-
    Remove dispatch-keyed Cursor and Grok termination handles after successful runtime handoff, termination, or cleanup so completed dispatches do not accumulate stale process handles. Preserve handles when cleanup remains unresolved, and cover both provider registries with focused size assertions.
