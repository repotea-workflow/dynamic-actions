Add Pi agent routing contract

codeon:
  version: 1
  authority: FWN-67
  description: |-
    Define the exact `pi` AgentKind wire value across canonical encoding, configuration, persistence, execution snapshots, and tracker MCP schemas.
    
    Add a data-safe four-table PostgreSQL migration, fail-closed codec coverage, Standard-only Goal validation, and ordinary operation callback catalog selection without adding Pi runtime transport.
