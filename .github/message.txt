fix(codeoff): bound required context callback (#17)

Prevent oversized required-history callbacks from failing after evidence is recorded. Return whole-message partial snapshots with explicit coverage and replay metadata at the Codex adapter boundary.
