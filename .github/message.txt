refactor(shell): drop filterGroup and move CLARITY grouping to style CEL

Shell data sources stay neutral; personal center sections filter via ClarityShellItemFilters instead of item.group or filterGroup.
