Allow Package blocked endpoints in blocker APIs

codeon:
  version: 1
  authority: fewensa-38
  description: |-
    Create already persisted Package blocked_by edges, but list/add/remove still rejected non-Task anchors, leaving durable edges unlistable and unremovable. Align those store paths and docs with Task-or-Package blocked endpoints while keeping Package-as-blocker rejected.
