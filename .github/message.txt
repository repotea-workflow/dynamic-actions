fix(codeon): harden Cursor self-recovery

Count continuation limits across consecutive stalled turns, distinguish the provider leader from orphan workers, and migrate stale v2 terminal evidence once.
