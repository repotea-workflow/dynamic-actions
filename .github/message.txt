fix(observability): redact execution paths

codeon:
  version: 1
  authority: FWN-58
  description: |-
    Project legacy operation, recovery, and runtime references through the public-safe reference boundary so local paths serialize only as deterministic digests.
    
    Expand external review rows into stable planned, requested, acknowledged, response-observed, and terminal timeline checkpoints so persisted lifecycle timestamps survive restart.
