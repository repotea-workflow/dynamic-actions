fix(waterfowl): unblock app_bar STAC package for feature style bundle build

Add missing app_bar_styles package metadata and replace unsupported
StacSizedBox.shrink so just build-feature-styles completes successfully.
