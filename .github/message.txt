fix(mobile): address GP printing test feedback

Map image brightness through identity at UI 0 so contrast-only edits no
longer crush scans to black. Expose black-mark locate only when the
catalog lists it, prefix schemeless web URLs with https://, keep
learning type switches on the overlay stack, and send wrong-question
retakes back to the camera.

Also adds taller NIB test pages, a single connect-error banner, junior
geography, delisted dead recommended sites, a Gprinter lockup, and
style-specific AI prompts.
