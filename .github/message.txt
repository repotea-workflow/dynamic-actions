fix(worker): stream identity ZIP exports

Large identity exports held the ZIP and multipart body in memory at the same time, causing repeated OOM restarts. Use temporary files for both stages so memory stays bounded to the current image and report.
