test(codeon): repair cancellation acceptance fixtures

codeon:
  version: 1
  authority: FWN-48
  description: |-
    Keep cancellation acceptance aligned with current durable authority: split multi-statement SQL setup, use current timestamps and Issue versions, reseal rewritten cleanup manifests, terminate active consumers through the cancellation lifecycle, and bound the cross-repository daemon test stack. This preserves exact provider isolation and cleanup/requeue evidence without changing product behavior.
