fix(mobile): align 错题本 to P0 mint and fix compose print

The capture banner uses the P0 mint teal gradient, not brand coral.
Subject cards use replica glyphs, the list is selectable cards, and
组卷打印 no longer treats saved images as camera captures (PNG was
labeled .jpg and surfaced as 拍照失败).
