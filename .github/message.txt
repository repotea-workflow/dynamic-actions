fix(runtime): refresh dispatch worker authority

codeon:
  version: 1
  authority: fewensa-16
  description: |-
    Refresh supervised dispatch worker authority immediately after durable claim acquisition while keeping duplicate admission keyed to stable dispatch identity. Resolve cancellation requests from the latest fenced dispatch state so newly checkpointed provider runtime and exact input references are used.
    
    Add focused supervisor and PostgreSQL acceptance coverage for refreshed fencing, bounded duplicate admission, durable lifecycle evidence, and latest-input cancellation authority.
