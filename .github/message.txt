fix(hawk): skip interpolated featureKey when collecting style dependencies.

dataRepeat templates bind openFeature at runtime via ${item.target.featureKey}; compile-time feature reference checks must ignore those placeholders.
