fix(robin): overlay platform resource properties without reflowing grid.

Keep the drive grid at full width when a file is selected by floating the properties panel over the right edge instead of sharing horizontal flex space.
