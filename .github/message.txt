Remove localization draft/publish workflow; save syncs runtime immediately.

Platform and App language packs no longer expose publish or version-list APIs. Saving, importing, or creating a pack writes the runtime version row automatically, and Robin drops the publish/history UI in favor of direct save and export.
