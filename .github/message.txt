fix(codex): order native profile globally

codeon:
  version: 1
  authority: FWN-61
  description: |-
    Codex parses --profile as a global option, so emit it before the app-server subcommand and lock the accepted order with a regression test.
