fix(codeon): bind e2e configs to keyed agent runtimes

codeon:
  version: 1
  authority: FWN-1
  description: |-
    Smoke and live E2E helpers now emit keyed agent_runtime_profiles with a mandatory runtime_profile binding so DaemonConfigSet::load matches the closed execution schema. Replace retired singleton provider tables and keep non-ignored config-load regression coverage for both entry points.
