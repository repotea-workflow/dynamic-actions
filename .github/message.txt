fix(codeon): bind Cursor continuation cancellation

codeon:
  version: 1
  authority: FWN-44
  description: |-
    Retain the continuation prompt operation ID alongside its returned prompt so session/cancel sends one internally consistent Cursor target.
    
    Extend the cross-Repository cancellation regression to trigger a completion continuation, verify the exact dispatch/session/prompt-operation/prompt tuple, and prove the other retained dispatch remains live.
