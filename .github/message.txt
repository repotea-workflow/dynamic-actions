fix(notification): harden delivery boundaries

codeon:
  version: 1
  authority: fewensa-29
  description: |-
    Remove the shared envelope's dependency on codeon-tracker while preserving the v1 wire contract.
    
    Persist dispatch-start authority and atomically seal post-dispatch lost fences as ambiguous before any newer claim can run. Reject exhausted claim attempts before provider dispatch and cover both behaviors with focused seam and transition tests.
