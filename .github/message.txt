fix(pi): bound response tracking and cleanup

codeon:
  version: 1
  authority: FWN-68
  description: |-
    Fence ID-less response correlation after its first completion and cap completed request IDs with a typed runtime limit.
    
    Confirm native process-group exit before cleanup succeeds, and bound transport thread joins by the graceful-close deadline. Add regressions for stale ID-less responses, completed-ID overflow, and a leader-exited RPC descendant retaining stdout.
