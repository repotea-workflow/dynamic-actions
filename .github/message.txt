fix(swan): render platform SVG icons and warm Clarity aliases at bootstrap

Use SvgPicture for managed file assets, preload platform icon aliases before first paint, and refresh the runtime snapshot so tab and menu glyphs display without a session.
