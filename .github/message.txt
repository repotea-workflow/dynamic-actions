fix(pi): close lifecycle recovery gaps

codeon:
  version: 1
  authority: FWN-70
  description: |-
    Clean up retained Pi resources when supervised model configuration fails.
    
    Track confirmed process cleanup separately so recovery can retry Extension deletion without inspecting an exited session. Preserve pending provider failures across cleanup activity until retry or successful-turn evidence supersedes them.
    
    Add focused regression coverage for all three lifecycle races.
