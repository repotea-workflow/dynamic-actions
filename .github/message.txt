docs(runbook): mark the GP test-feedback plan as a 2026-08-29 snapshot

The original writeup was assignment-ready against that day's main. Later
PRs landed most of the confirmed defects, so keep the investigation and
point readers at regression instead of re-implementation.
