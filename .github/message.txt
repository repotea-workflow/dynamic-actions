fix(banners): repair form and image uploads (#122)

Allow home_banner assets through database media constraints so upload intents can persist. Make optional form fields explicit and keep newest banners first.
