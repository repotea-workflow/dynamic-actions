fix(robin): scroll properties panel content within workspace viewport

Keep the sticky shell within the visible page area and scroll only the panel body so save/delete actions stay reachable below the shell header.
