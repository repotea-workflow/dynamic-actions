fix(notification): enforce canonical v1 inputs

codeon:
  version: 1
  authority: fewensa-28
  description: |-
    Normalize validated HTTPS URLs before storing and encoding them, and require namespace-qualified event names.
    
    Add exact Codeoff canonical bytes, digest, and replay coverage alongside normalized URL regression coverage. Document the notification crate in the OpenWiki workspace ownership maps.
