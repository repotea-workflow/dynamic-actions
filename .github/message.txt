fix(swan): stabilize shell startup and offline shell UI rendering.

Hydrate release theme and shell styles before first frame, keep home and personal center on feature styles instead of release payloads, and fall back to bundled shell assets when Hawk manifest data is missing.
