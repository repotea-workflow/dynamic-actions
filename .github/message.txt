fix(runtime): fence queued lease activations

codeon:
  version: 1
  authority: FWN-51
  description: |-
    Exclude Attempts with queued runnable-lease activations from the generic execution-runtime candidate query so bounded activation delivery remains the only path that can start resumed work. Extend the existing lease lifecycle integration scenario to verify the queued Attempt is not returned as a generic candidate.
