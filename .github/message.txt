fix(mobile): harden web print URL, back, and load errors

Prefix schemeless addresses with https://, pop WebView history
before leaving the feature, and replace blank main-frame failures
with an honest retryable error page. Keep dead recommended remotes
delisted rather than weakening TLS.
