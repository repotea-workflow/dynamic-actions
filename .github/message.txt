feat(support): dispatch host export formats

Add PlateHostController.exportDocumentPayload(format: ...) so support owns host export format dispatch, and update plate-editor-host to delegate the message format to the SDK.
