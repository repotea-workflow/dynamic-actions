fix(tracker): seal lifecycle v2 boundaries

codeon:
  version: 1
  authority: fewensa-12
  description: |-
    Derive adapter-facing runtime call deadlines from sealed v2 execution policy while preserving explicit v1 fixed-deadline compatibility.
    
    Render renewable activity leases and all control-call deadlines in v2 operation prompts without changing v1 canonical output. Add a tracker-owned persistence reader boundary for external activity evidence rehydration.
