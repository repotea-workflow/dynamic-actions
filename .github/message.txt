Fix Pi recommended Goal fallback

codeon:
  version: 1
  authority: FWN-67
  description: |-
    Seal CapabilityUnsupported when the default daemon policy resolves a recommended Pi Goal to Standard execution. Add an end-to-end regression that builds the default Pi policy snapshot, renders the operation input, and validates the binding before runtime dispatch.
