fix(codeon): harden activity catalog decoding

codeon:
  version: 1
  authority: fewensa-34
  description: |-
    Prevent schema-drift diagnostics from including input-derived serde details, and enforce the producer-specific required and null invariants for finalization activity contexts.
    
    Restore the existing public CurrentExecutionStatus contract while retaining catalog-local typed terminal activity statuses and the original persisted terminal event meanings. Add focused regression coverage for redaction canaries, malformed finalization variants, and public API compatibility.
