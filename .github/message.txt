fix(runtime): fence lease resume authority

codeon:
  version: 1
  authority: FWN-51
  description: |-
    Bind release-capable containment evidence to the locked attempt, operation, latest dispatch, and canonical persisted containment readback before releasing runnable capacity.
    
    Derive resume capacity requirements from the locked operation kind, and carry the exact operation ID and version through activation claim, delivery, worker admission, and pre-I/O runtime fencing.
    
    Add PostgreSQL and daemon regression coverage for cross-dispatch evidence rejection, stale activation versions, and operation-version worker fencing.
