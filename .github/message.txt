test: add missingCatalog E2E script and Hawk integration coverage

Add a dev API E2E script that exercises preview, register-catalog, and
re-preview with unique probe IDs, and expand dynamic_app_db tests for
action and component missingCatalog registration without requiring the
release compiler fixture.
