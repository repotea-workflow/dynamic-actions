fix(grok): persist activity recovery fences

codeon:
  version: 1
  authority: FWN-13
  description: |-
    Add a controller-authorized, generation-idempotent activity lease rearm transition for exact Grok recovery. The transition advances only the idle deadline, preserves the frozen resource ceiling, and refuses recovery when either returned deadline is already due.
    
    Persist fenced closed-generation provider evidence as bounded audit-only dispatch events so late and duplicate activity remains restart-safe without renewing the lease. Cover recovery, ceiling, duplicate, migration, and observability behavior with focused fake ACP and PostgreSQL tests.
