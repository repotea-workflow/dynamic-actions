fix(tracker): enforce routing activity invariants

codeon:
  version: 1
  authority: fewensa-34
  description: |-
    Reject issue.created contexts unless routing keys exactly match binding keys, and reject routing replacements with no changed keys or unchanged policy digests. Add focused schema-drift fixtures for each invalid shape.
    
    Remove the candidate-added PostgreSQL execution activity writes and acceptance assertions so the catalog revision remains tracker-owned and existing Issue activity feed behavior stays compatible.
