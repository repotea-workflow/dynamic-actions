fix(runtime): fence ambiguous lifecycle recovery

codeon:
  version: 1
  authority: FWN-18
  description: |-
    Treat requested Codex interrupts after lifecycle expiry as unknown so stalled turns enter Attention without polling-based renewal.
    
    Stop Cursor restart recovery from dispatching a continuation when exact session load cannot prove prior prompt state, while preserving completion-race acceptance and removing obsolete recovered-clock scaffolding.
    
    Reject profile-only lifecycle v2 canaries and document the safe Cursor restart boundary.
