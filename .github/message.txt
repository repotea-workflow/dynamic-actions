feat(admin): complete management console flows (#16)

* feat: add workspace selection flow

Expose tenant memberships separately from App access so accounts can choose among platform and tenant workspaces.

* perf(robin): lazy-load route modules

* feat(management): add resource URL codes

Backfill stable tenant and App codes for readable URLs while preserving legacy numeric ID access.

* fix(robin): expand nav groups from full row

* refactor(management): rename URL codes to slugs

Add a forward migration so databases that already applied resource codes preserve migration history and upgrade safely.

* feat(robin): standardize admin interactions

* feat(admin): refine management interactions

* feat(identity): add role management flow

* feat(api): publish split OpenAPI specs

Replace checked-in OpenAPI artifacts with Hawk management/runtime endpoints and generated Robin/Swan clients.

* feat(swan): add compiled component bundles

Keep runtime Stac payloads limited to components selected by each Shell binary.

* chore(dev): align management origin

---------

Co-authored-by: 0x128bit <306828631+0x128bit@users.noreply.github.com>
