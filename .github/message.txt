fix(codeon): recover signing preparation

Missing signing configuration previously stranded attempts before any operation existed. Retry before Git mutation and requeue exact restart-safe recoveries on the same issue.
