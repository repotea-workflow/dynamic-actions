feat(codeoff): add scheduled GitHub writes

Keep scheduled mutations behind an independent default-off switch and an exact server-side allowlist. Reject destructive upstream annotations and preserve action-ledger replay fencing.
