Fix style sync error mapping and refresh coverage after sync.

Return a dedicated conflict issue when feature styles disagree on fallback text, and invalidate the Robin coverage query after a successful sync.
