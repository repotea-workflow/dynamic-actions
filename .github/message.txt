fix(runtime): isolate worker faults

codeon:
  version: 1
  authority: FWN-16
  description: |-
    Merge the refreshed target branch without discarding either runtime lifecycle path.
    
    Validate the live migration ledger from the execution runtime so global persistence corruption signals ProcessFatal while transient persistence and provider-local invariants remain isolated.
    
    Exercise dispatch-worker panic recovery through the execution service and verify Picker and MCP remain responsive.
