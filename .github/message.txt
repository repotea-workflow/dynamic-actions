fix(postgres): freeze notification evidence

codeon:
  version: 1
  authority: FWN-33
  description: |-
    Protect notification outbox enqueue-time fields and completed delivery attempts from later database updates. Require a strictly unexpired lease before marking provider dispatch, and cover immutability, lease-boundary recovery, and frozen deadline policies in PostgreSQL acceptance tests.
