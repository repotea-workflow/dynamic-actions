fix(mobile): ship distinct AI print style prompts (#157)

Server CreateAiGenerationJobRequest has no systemPrompt, so style only
reached the model as a short user-prompt tag. Put versioned style
directives into the shipped prompt and assert the request JSON prompt
differs for each style.
