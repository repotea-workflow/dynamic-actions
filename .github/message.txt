fix(runtime): close lifecycle v2 revision gaps

codeon:
  version: 1
  authority: FWN-18
  description: |-
    Persist Cursor resource-ceiling continuation intent before provider send and return Unknown on replay or persistence ambiguity without resending. Validate lifecycle-v2 profile canaries against loaded profiles, align the checked-in rollout example, and update the runtime specification.
