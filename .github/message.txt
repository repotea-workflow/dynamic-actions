fix(grok): persist lifecycle recovery evidence

codeon:
  version: 1
  authority: FWN-13
  description: |-
    Commit activity lease rearm state before exact Grok recovery while preserving the original resource ceiling and continuation count.
    
    Persist fenced late-generation provider evidence durably, and drain close-time provider activity after the provider fence before final cleanup and capability release.
    
    Add exact-resume, cleanup-race, and PostgreSQL lifecycle regression coverage.
