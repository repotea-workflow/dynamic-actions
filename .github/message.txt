Resolve CLARITY theme icons from assets/themes/{key}/ convention.

Replace per-theme assetOverrides with path-based lookup in Hawk runtime and Swan STAC loading, drop the theme assets API/table, and update Robin and seed scripts accordingly.
