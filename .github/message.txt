Keep Pi lifecycle work within runner scope

codeon:
  version: 1
  authority: FWN-70
  description: |-
    Supervise the settled final-text diagnostic through the provider blocking-call supervisor so a missing Pi response cannot block an async daemon worker.
    
    Restore the prerequisite Pi Adapter and Extension implementation unchanged, keeping cleanup retry behavior and redacted provider-failure handling local to the operation runner.
