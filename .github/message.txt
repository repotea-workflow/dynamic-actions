fix(templates): isolate Plate editor

Run the Flutter host in an opaque-origin sandbox and expose only the validated postMessage bridge. Preserve guarded browser history and freeze the editor during sign-out. Lock Flutter dependencies for reproducible CI builds.
