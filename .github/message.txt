Fix Pi lifecycle review findings

codeon:
  version: 1
  authority: FWN-70
  description: |-
    Clear the ambient environment for Pi version and RPC child processes while preserving only the explicit bootstrap allowlist.
    
    Restore redacted provider failure categories so authentication and exhausted rate limits map correctly. Retain cancellation cleanup ownership when capability revocation fails so same-daemon recovery can retry finalization.
