feat(tracker): add typed activity catalog

codeon:
  version: 1
  authority: fewensa-34
  description: |-
    Decode the complete SQL activity event set through closed tracker-domain contracts so notification consumers cannot match arbitrary strings or silently ignore schema drift.
    
    Project execution updates into stable bounded semantic events while preserving source event identity and keeping routing and delivery out of scope.
