fix(grok): preserve activity recovery authority

codeon:
  version: 1
  authority: FWN-13
  description: |-
    Drain and normalize bounded provider activity from pending ACP prompt responses, including first-event deduplication and active prompt binding.
    
    Retain completion context and MCP capability until provider session cleanup is proven, and require controller recovery when a retained session lacks accepted-input authority.
    
    Remove the candidate-only shared activity lease rearm migration and persistence mutations so Grok continues to consume the existing lifecycle v2 controller authority.
