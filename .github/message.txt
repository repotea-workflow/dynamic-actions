Unify STAC runtime limits with component-depth semantics.

Introduce a single Baya manifest for payload, envelope, and depth limits, then align Hawk, stac_adapter, and release validation on generated constants with separate component and JSON depth checks.
