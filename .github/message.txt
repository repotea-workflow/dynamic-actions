fix(mobile): attribute NIB disconnects and keep a single connect error

Record a monotonic session log for discovery, connect, writes, lifecycle,
and typed disconnect reasons so printer drops can be diagnosed from logs.
Retry a write timeout once while the link still reports connected.

Bind print readiness to the model used at connect without falling back to
the selected catalog, and suppress the empty-printers banner when a
failure is already visible.
