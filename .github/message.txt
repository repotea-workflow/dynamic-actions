fix(codex): initialize turn MCP catalogs

Wait for invocation-scoped MCP servers to load their tool catalogs before starting the Codex thread. This prevents scheduled runs from completing without their authorized tools.
