fix(api): tolerate additive timeline kinds

codeon:
  version: 1
  authority: FWN-59
  description: |-
    Project unknown additive timeline event kinds through the documented safe fact payload instead of failing the entire timeline read. Keep known operation state transitions and known fact status validation strict, and cover the PostgreSQL-to-HTTP path with generated-client deserialization.
