fix(runtime): repair recovery fence tests

codeon:
  version: 1
  authority: FWN-51
  description: |-
    Move the Codex stale-generation rejection counter to the runtime lease validation callback that recovery invokes. Assert that Cursor recovery creates no fake-agent report when the generation fence prevents process launch.
