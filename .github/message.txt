fix(grok): retain startup cleanup authority

codeon:
  version: 1
  authority: FWN-13
  description: |-
    Route failed Grok startup cleanup through the shared runtime resource closer so completion context and MCP capability release happen only after provider cleanup is proven.
    
    Add regressions for ambiguous startup cleanup and periodic text/tool activity beyond 30 minutes followed by silent idle expiry.
