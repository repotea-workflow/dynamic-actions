fix(hawk): refresh stale compiled styles before a Dev snapshot

Bundle import was rolling back when an unchanged style's compiled
payload lagged the current compiler, so no styles or Revisions landed.
