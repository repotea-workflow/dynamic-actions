fix(notification): bound post-dispatch store waits

codeon:
  version: 1
  authority: fewensa-29
  description: |-
    Poll lease heartbeats alongside provider send, cancellation, and send timeout so a stalled store heartbeat cannot strand dispatched work.
    
    Bound final heartbeat and completion transitions with a frozen finalization timeout, conservatively recording durable ambiguity when a post-dispatch transition stalls. Preserve lost-fence sealing and cover stalled heartbeat, cancellation, completion, and final-heartbeat fence loss.
