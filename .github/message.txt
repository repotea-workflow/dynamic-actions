fix(runtime): fence observation persistence atomically

codeon:
  version: 1
  authority: FWN-16
  description: |-
    Carry the worker's dispatch ID and fence into running-observation persistence and validate that authority under the same PostgreSQL transaction as the operation mutation. Treat a fence race as scheduler contention and add a barrier-based regression test that advances the fence after inspection readback but before the write.
