fix(grok): preserve lifecycle v2 recovery

codeon:
  version: 1
  authority: FWN-13
  description: |-
    Treat the first prompt-bound provider event as prompt acknowledgement while retaining the pending ACP call for inspection.
    
    Normalize prompt-local provider sequences without placing them in the dispatch-wide monotonic sequence field, while preserving raw sequence identity in the event digest.
    
    Carry exact Grok session, prompt, generation, and continuation authority through production recovery so closed generations load exactly without resetting lifecycle state.
