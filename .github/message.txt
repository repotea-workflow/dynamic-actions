fix(robin): add editable scope tag fields in platform resource properties.

Users could add scope tags but had no inputs for tenant, app, or note; the panel now renders a proper editor aligned with backend validation.
