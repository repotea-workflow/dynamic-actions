fix(dynamic): harden release delivery

Compile releases from immutable compositions, persist crash-safe slots, and keep release storage aligned with the existing schema and runtime permission boundaries.
