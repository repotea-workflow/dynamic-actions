fix(codeoff): reconcile terminal notifications

codeon:
  version: 1
  authority: FWN-30
  description: |-
    Persist enqueue receipts atomically with notification outbox rows and reconcile committed scheduler terminal states that lack receipts. This preserves scheduler authority while making run and delivery terminal notifications retryable across transient enqueue failures, restarts, and outbox retention.
