feat: resolve commit authority aliases by Issue identity

codeon:
  version: 1
  authority: fewensa-24
  description: |-
    Parse codeon.authority as an IssueReference so prefix and workspace-key forms stay syntax-valid. Resolve those aliases at the store boundary against Profile, Workspace, and Attempt context, and authenticate them only when they map to the same IssueId. Generate new requirements, preparations, and messages with the preferred uppercase prefix while keeping historical key metadata, sealed receipts, and canonical bytes exact.
