fix(grok): preserve activity before cleanup

codeon:
  version: 1
  authority: FWN-13
  description: |-
    Persist retained Grok provider activity before completion-triggered cleanup so transient persistence failures can retry without losing evidence.
    
    Remove the issue-owned lifecycle rearm migration and shared PostgreSQL APIs, restoring controller-required recovery until the blocked persistence dependency lands.
