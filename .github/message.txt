fix(swan): keep personal center stable when returning from feature pages

Use a dedicated shell tab with cached runtimeShellStyleProvider instead of
FeaturePage FutureBuilder reloads, pad single-tab app bars for NavigationBar,
and defer feature page activation to postFrameCallback. Split app_router into
focused modules to stay within codeline limits.
