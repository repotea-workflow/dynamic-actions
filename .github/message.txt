fix(codeoff): preserve scheduler notification authority

codeon:
  version: 1
  authority: FWN-30
  description: |-
    Accept the scheduler's bounded Unicode run IDs and maximum encoded delivery IDs in the shared Codeoff notification authority contract. Keep exact identifiers in semantic envelopes and durable enqueue receipts while using Unicode-safe fact previews and a domain-separated digest for header-unsafe authority metadata. Add boundary, all-terminal-event, and restart reconciliation regressions, and document the projection behavior.
