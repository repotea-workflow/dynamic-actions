fix(pi): preserve recovery completion authority

codeon:
  version: 1
  authority: FWN-70
  description: |-
    Return terminal recovery whenever cleanup readback finds an accepted completion, and cover both same-daemon cleanup race branches.
    
    Restore the dependency-owned Pi protocol API and keep cancellation-only inspection coordination local to the Pi runner.
