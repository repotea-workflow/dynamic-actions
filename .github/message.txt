fix(codeon): preserve runtime auth failures

Persist provider-neutral authentication evidence and recover terminal Codex input without duplicating dispatches. Isolate ambient Codex capabilities and require authoritative single-turn readback.\n\nAdds the input-sending terminal readback migration.
