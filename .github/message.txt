Fail closed on unsealed Agent capabilities

codeon:
  version: 1
  authority: FWN-62
  description: |-
    Reject non-empty Agent Capability Manifests until provider injection and readback are implemented, and keep preflight-only evidence from claiming an observed inventory digest.
    
    Quarantine active validator-v2 bindings into durable recovery during migration 0132, and reserve the exact codeon-operation-completion server and qualified tool namespace across configuration, domain, and PostgreSQL validation.
    
    Align the runtime specification and OpenWiki summary with the fail-closed boundary.
