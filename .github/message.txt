perf(text): avoid embedded-font drag stalls

Keep URL fonts external by default and embed bytes only when explicitly requested for export. Cache the selected SVG and route move deltas through the interaction overlay so large font payloads stay out of the drag loop.
