feat: replace missingActions with missingCatalog for bundle import

Align feature-style bundle preview and registration with Cosmos: unified
missingCatalog entries for actions and components, register-catalog API,
Robin import UI updates, and CLI confirmation before auto-registration.
Split management catalog module to satisfy codeline limits.
