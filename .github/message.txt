fix(codeon): restore fmt and document smoke DB default

codeon:
  version: 1
  authority: FWN-1
  description: |-
    Apply rustfmt to the shared E2E test helper so fmt-check passes again, and document that smoke-e2e auto-provisions local PostgreSQL when CODEON_TEST_DATABASE_URL is unset, with the isolated URL as an override.
