revert(codeon): drop unverified recovery mix

Keep the verified LandingPreflight and verification-output fixes from #61, but remove the unrelated recovery cancellation transaction and prompt changes until they have dedicated invariant coverage.
