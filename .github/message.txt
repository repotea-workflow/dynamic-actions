fix(postgres): preserve legacy Attention intents

codeon:
  version: 1
  authority: FWN-60
  description: |-
    Keep migration 0129 compatible with pre-existing select_observed_candidate resolution intents whose new candidate-reference column is null, while retaining constraint enforcement for new writes.
    
    Add PostgreSQL migration acceptance coverage that seeds a pre-0129 selection intent, verifies the upgrade preserves it, and confirms a new selection intent without a candidate reference is rejected.
