fix cancellation recovery cleanup safety

codeon:
  version: 1
  authority: FWN-46
  description: |-
    Authenticate cancellation remote readback with the profile Git credential and fail closed when it is unavailable.
    
    Check registered worktree users immediately before deleting the owned local branch, preserving the ref and opening AuthorityDrift when another worktree still uses it.
    
    Add focused regression coverage for both sealed findings.
