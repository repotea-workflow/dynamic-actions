feat(text): add glyph layout snapshots

Use stable text-run identities and ABI5 glyph snapshots so committed previews and exports share authoritative native layout. Bound snapshot caching by memory and fingerprint layouts with exact font bytes and artifact identity.
