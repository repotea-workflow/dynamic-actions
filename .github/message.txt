fix(runtime): preserve lifecycle recovery authority

codeon:
  version: 1
  authority: FWN-18
  description: |-
    Keep the original resource ceiling unchanged when recovered activity re-arms an idle lease. Retain drained Grok activity and reconciliation state across transient acceptance failures so an empty follow-up inspection can safely retry the exact evidence.
