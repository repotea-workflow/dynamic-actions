Merge origin/main and isolate runtime profiles

codeon:
  version: 1
  authority: FWN-61
  description: |-
    Resolve the frozen branch against refreshed origin/main while preserving both Agent Runtime Profile behavior and the target branch's Attention changes.
    
    Defer invalid per-profile state paths to isolated runtime composition while retaining overlap checks for successfully resolved homes. Remove the process-wide Codex protocol-mode override so keyed configuration and sealed snapshots agree. Renumber the unlanded Agent Runtime Profile migration to 0130 after the target branch's established 0124-0129 migrations.
