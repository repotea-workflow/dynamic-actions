feat(robin): upload App files from the file library page

Wire PNG/JPEG uploads through the Hawk files presign/finalize API so
Robin can populate the App file library instead of only listing aliases.
