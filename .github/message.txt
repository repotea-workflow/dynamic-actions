Enforce effective blockers across execution boundaries

codeon:
  version: 1
  authority: fewensa-40
  description: |-
    Wire the canonical effective-blocker projection through Picker filtering, claim recheck, Admission snapshot evidence, dispatchability fingerprints, late finalization validation, and Package aggregation so Parent Package blockers cannot be bypassed at eligibility or completion.
