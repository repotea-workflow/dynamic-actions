fix(codeon): use valid live-e2e Issue priority

codeon:
  version: 1
  authority: FWN-1
  description: |-
    Replace the invalid MCP priority "normal" with "medium" so live Issue creation passes schema validation, and lock the live Issue-create payload against the priority enum with a unit regression test.
