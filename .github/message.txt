fix(postgres): bind notification evidence

codeon:
  version: 1
  authority: fewensa-33
  description: |-
    Enforce notification outbox source lineage against the authoritative Issue event, align persisted identifiers with the shared store contract, bind rendered metadata to its semantic envelope, and reject partial delivery receipt states.
    
    Add focused PostgreSQL coverage for contradictory lineage, leading punctuation, metadata mismatch, and partial receipt shapes.
