refactor: split device material pages by manuals and tutorials

Keep the route entry as a thin re-export and move shared upload/lifecycle
helpers into a dedicated module so the page file stays under the size limit.
