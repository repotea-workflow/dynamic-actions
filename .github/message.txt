refactor(swan): use OpenAPI models in app data source payloads

Keep payload classes only for STAC envelope conversion and serialize RuntimeProfile, RuntimeBanner, and RuntimeManifestFeature via standardSerializers instead of duplicating field models.
