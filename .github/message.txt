fix(mobile): localize editor chrome and keep zh as Hans (#148, #149) (#174)

Move graphic/note editor chrome off zh/en _label helpers onto ARB-backed
AppLocalizations. Plate can hide the app delegate, so the shell restores
the ambient locale around the canvas.

Rewrite app_zh.arb as a Simplified fallback matching Hans so generic zh
no longer ships mixed Traditional copy. Runtime still maps zh to Hans.
