fix(live-e2e): escape TOML config and scope cleanup readback

codeon:
  version: 1
  authority: FWN-1
  description: |-
    Serialize live daemon config strings through TOML escaping so worktree paths and profile display names with quotes or backslashes load correctly.
    
    Assert cleanup against the run-owned preparation worktree_path from operation history instead of requiring an empty worktree_root directory, and add regression tests for both findings.
