feat(notification): add Webhook provider

codeon:
  version: 1
  authority: fewensa-31
  description: |-
    Add application-neutral Markdown and plaintext rendering with replay-bound metadata and exact frozen-byte delivery.
    
    Resolve endpoints and bearer credentials only at send time, enforce HTTPS with explicit loopback-only HTTP tests, bound responses and Retry-After, and classify confirmed, retryable, terminal, and ambiguous outcomes.
    
    Cover exact bodies and headers, authentication, limits, failures, redaction, and semantic-core isolation with focused mock-server tests and document the provider contract.
