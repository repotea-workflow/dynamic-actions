refactor(auth): simplify MCP access leases (#111)

Authorize upstream MCP calls by account, repository, and coarse access level instead of persisted tool arguments. Keep legacy plan and scheduler snapshots readable while preserving approval, audit, and durable run boundaries.
