fix(runtime): preserve ambiguous startup leases

codeon:
  version: 1
  authority: FWN-51
  description: |-
    Require terminal Agent ownership evidence before startup treats a current SCM phase as runtime-not-applicable. Preserve the held lease and enter Recovery Required when the Agent dispatch for that generation is non-terminal, and cover the fresh-empty containment regression.
