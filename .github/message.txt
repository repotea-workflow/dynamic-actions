fix(recovery): replay worker completions

Distinguish operation, attention, and execution-flow recovery evidence so restart finalization restores only the matching Attempt authority.

Add a guarded Revision replay migration and preserve microsecond precision in runnable-lease event timestamps.
