Refactor GitHub authorization around resource scopes

codeon:
  version: 1
  authority: fewensa-4
  description: |-
    Replace flattened repository-list grants with correlated tagged permission clauses and a single policy-owned route proof.
    
    Owner bypasses ordinary role clauses only; every caller, including Owner and temporary approval, still needs a containing configured account route whose max_access is sufficient. Auto account selection uses configured routes rather than public repository readability. Tool lists remain compatibility-only narrowing evidence.
    
    Validation: cargo test -p codeoff-runtime; cargo test -p codeoff-config; cargo test -p codeoff-mcp; cargo test -p codeoff-cli; cargo fmt --all -- --check; cargo clippy -p codeoff-runtime -p codeoff-config -p codeoff-mcp -p codeoff-cli --all-targets -- -D warnings.
