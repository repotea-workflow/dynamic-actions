Fail closed on unsupported result coordinates

codeon:
  version: 1
  authority: FWN-6
  description: |-
    Treat repo/repos/repositories and organization collection keys as lease-checked coordinates, validate string collection elements, and reject unverifiable scalar shapes so approved pointers cannot mask foreign repository data. Format candidate-owned MCP containment and proxy test files for rustfmt.
