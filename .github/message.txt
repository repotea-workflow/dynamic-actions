fix(pi): bound version probe output

codeon:
  version: 1
  authority: FWN-68
  description: |-
    Read version stdout under the startup deadline so a descendant retaining the pipe cannot block session startup. Kill and reap the isolated probe process group before returning.
