fix(hawk): accept dataRepeat template interpolations during style validation.

Skip catalog schema checks for ${item.*} placeholders in action arguments and allow dynamic actionType bindings resolved at runtime.
