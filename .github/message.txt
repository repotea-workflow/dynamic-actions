fix(codeon): seal smoke runtime authority and live Codex homes

codeon:
  version: 1
  authority: FWN-1
  description: |-
    SmokeScripted now advertises the (Codex, smoke) identity and seals a synthetic runtime-profile snapshot so binding creation can enter Agent-owned Operations. Live preflight validates every generated Codex/Cursor/Grok runtime home or credential env before provider work, and the runbook documents the required exports. Also restore rustfmt on the smoke config assertion and unwrap the notification TestDatabase Result so lib tests compile.
