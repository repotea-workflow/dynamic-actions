fix(build): harden sandbox and iOS export

Require an isolated source build before publishable signing, export iOS IPAs with the configured profile, and keep legacy migration rows explicitly unsnapshotted.
