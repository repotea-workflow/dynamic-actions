fix(robin): dedupe shell entries and hide icon column

Filter configured features and built-ins out of add selectors, toast on
duplicate attempts, and drop the icon column from the shell studio table.
