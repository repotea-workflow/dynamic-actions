fix(runtime): replay terminal dispatch completions

codeon:
  version: 1
  authority: FWN-16
  description: |-
    Skip runtime worker start evidence when startup recovery loads a terminal dispatch. This preserves refreshed dispatch and fence authority while allowing the durable completion replay path to run; nonterminal workers retain the existing lifecycle evidence.
