feat(platform): sync style fallbacks into primary language pack on import.

Align Aviary feature style import with Cosmos by defaulting missing localization keys into the primary platform pack draft, and expose the same opt-out in Robin and the import CLI.
