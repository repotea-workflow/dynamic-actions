fix(postgres): move enum codec tests last

codeon:
  version: 1
  authority: FWN-58
  description: |-
    Move the enum codec test module after all production items so the repository's items-after-test-module Clippy lint no longer rejects the target file.
    
    The focused landing-preflight result decoder test remains unchanged and passes. The required all-target Clippy command proceeds past this finding but remains blocked by an unrelated pre-existing notification_persistence.rs type mismatch outside the sealed revision scope.
