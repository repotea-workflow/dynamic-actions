fix(cursor): preserve recovery acceptance history

codeon:
  version: 1
  authority: FWN-62
  description: |-
    Scope Cursor capability acceptance evidence references to each observation so exact-session recovery appends a new durable history row instead of conflicting with initial provisioning evidence.
    
    Extend the PostgreSQL binding recovery coverage to persist two observations for the same Cursor session and verify both history rows and latest-observation readback.
