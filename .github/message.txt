feat: add backend-managed home banner carousel (#120)

Publish home hero and secondary banners from the admin console,
including images, locales, app or https links, schedules, and
device filters. The app keeps the current three bundled slides as
the fallback when the public catalog is empty or unreachable.
