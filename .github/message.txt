feat(robin): share one tenant and App distribution picker

Give Releases, banners, features, styles, and device models the same DistributionTargetEditor and copy so operators pick all tenants or selected tenants and Apps in one way.
