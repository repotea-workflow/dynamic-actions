test(codeon): provision run-scoped test databases

Support isolated external admin URLs and local temporary PostgreSQL with
exact-name cleanup, so smoke-e2e no longer depends on a pre-shared recipe
database requirement.

Co-authored-by: Cursor <cursoragent@cursor.com>
