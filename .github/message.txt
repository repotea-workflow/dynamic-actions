fix(mobile): match Home layout proportions to the P0 replica (#165)

Replace GridView aspect-ratio stretching with P0 replica tokens so
module cards stay short and wide, minor tools keep intrinsic height,
the connect chip is dot+text+chevron, and banner/cards use palette
radius plus replica shadow.

Closes #133
