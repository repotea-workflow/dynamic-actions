fix(swan): clear context on session loss

Discard cached permission and feature context whenever Hawk revokes or invalidates the runtime session so stale user state cannot continue driving expressions.
