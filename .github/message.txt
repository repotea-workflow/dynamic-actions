fix(mobile): match P0 print preview controls and thumbnail border (#178)

Keep the image-print thumbnail's 2px mint/coral stroke outside the
inner clip so rounded corners no longer eat the frame. Print preview
uses the P0 printer glyph, 分页纸/连续纸, always-visible 方向, keeps
图像, drops 纸张尺寸, and paints the same brand stroke on the
settings card.
