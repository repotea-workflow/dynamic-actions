Reject organization-scoped GitHub CLI execution

codeon:
  version: 1
  authority: FWN-6
  description: |-
    Organization leases share the CLI bearer but argv validation only constrains -R/--repo, so global search and --org selectors could read cross-organization data. Fail closed for organization scopes until CLI has equivalent structured containment, and cover the leak paths with HTTP regressions.
