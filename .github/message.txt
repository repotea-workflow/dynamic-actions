fix(codeon): support blocking callbacks

Codex persistence callbacks use block_in_place, which panics on the daemon current-thread runtime. Run the daemon on a bounded multi-thread runtime and lock the contract with a regression test.
