fix(runtime): preserve profile migration evidence

codeon:
  version: 1
  authority: FWN-61
  description: |-
    Preserve bounded runtime observation evidence while quarantining legacy Agent Runtime Profile bindings. Append recovery lifecycle events for pending and running Operations, retain an immutable migration addendum for Operations already in recovery, and cover all three migration states with PostgreSQL acceptance. Integrate the refreshed target branch while retaining provider-scoped runtime isolation and target-side lifecycle fixes.
